#include <bits/stdc++.h>
using namespace std ;
#define int long long
void solve()
{
    int n;
    cin >> n ;
    vector <int> v (n) ;
    for (int i = 0 ; i < n ;i++) cin >> v[i] ;
    vector <int>dp(n+1) ;
    for (int i = 1 ;i < n ; i++)
    {
        
    }
}
signed main ()
{
    int t ;
    cin >> t ;
    while (t--)
    solve ();
}