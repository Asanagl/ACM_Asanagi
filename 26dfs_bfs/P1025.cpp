#include <bits/stdc++.h>
using namespace std ;
int main ()
{
    int n , k ;
    cin >> n >> k ;
    vector <int> dp (10 , 0) ;
    for (int i = 1 ; i <= k ; i++)
    {
        dp[i] = max (dp[i-1])
    }
}